#include "../lib/ShaderAPI.h"

shader apVectorLength
[[
    string help = "Return length of a vector",
]]
(
    vector inputV = vector(0.0),

    output float outputF = 0.0
)
{
    outputF = length(inputV);
} 