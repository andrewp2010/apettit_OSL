#include "../lib/ShaderAPI.h"
#include "../lib/Color.h"

shader apColor_1
[[ string help = "Simple color pass-through operation" ]]
(
    color inputRGB = COLOR_DB
    [[
        int connectable = 0,
        string label = "RGB"
    ]],
    
    COLOR_OUTPUTS
)
{
    resultRGB = inputRGB;
    COLOR_OUTPUTS_SET;
} 